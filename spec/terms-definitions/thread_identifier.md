[[def: thread identifier, Thread identifier, thread identifiers]]

~ An optional string carried in the `threadId` member that correlates a *Trust Task document* with other documents belonging to the same logical exchange. See [The `threadId` Member](#the-threadid-member).
