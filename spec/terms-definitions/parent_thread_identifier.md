[[def: parent thread identifier, Parent thread identifier]]

~ An optional string carried in the `parentThreadId` member that names the exchange containing this one, where a *Trust Task* is conducted inside a broader exchange. See [The `parentThreadId` Member](#the-parentthreadid-member).
